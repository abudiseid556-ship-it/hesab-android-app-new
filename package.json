{
  "name": "hesab-accounting-app",
  "version": "1.0.0",
  "private": true,
  "description": "ሂሳብ ማወራረጃ - Android app",
  "scripts": {
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:build": "npx cap build android"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/android": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.9.2"
  }
}
